# Inline Homebrew Tap

This tap provides the Inline CLI cask.

## Install

```bash
brew tap inline-chat/homebrew-inline
brew install --cask inline
```

## Upgrade

```bash
brew upgrade --cask inline
```
